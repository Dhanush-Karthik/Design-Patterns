package com.examples.patterns.creational.factory;

/**
 * Product interface declares the operation(s)
 * that all concrete products must implement.
 */
public interface Product {
    void runProduct();
}
